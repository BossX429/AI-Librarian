# AI-Librarian Requirements
# Core dependencies for conversation capture and management

# UI Automation for Claude Desktop
pywinauto>=0.6.8
psutil>=5.9.0
pygetwindow>=0.0.9
pywin32>=305

# Data processing
# (All built-in: json, sqlite3, pathlib, datetime, difflib)

# Optional: For advanced features
# pytest>=7.0.0  # Uncomment for testing
# pylint>=2.0.0  # Uncomment for code quality checks
